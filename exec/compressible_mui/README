To compile SPPARKS (KMC codes): 
- cd SPPARKS_MUI/
- make mui
(if you want to recompile the codes completely, try this first: make clean-all)

To comple FHD codes:
- make
(if you want to recompile the codes completely, try this first: make realclean)

To run example
- ./run.sh
(if RUN directory already exists, delete or rename it.)
